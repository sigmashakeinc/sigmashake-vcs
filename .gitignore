node_modules/
.wrangler/
dist/

# Autopilot-generated governance metadata lives in .sigmashake/ trees;
# these are regenerated by the host machine and must not be committed
# into worker projects (else they'd ship to the asset CDN).
**/.sigmashake/
